## Pre-spike snapshot — 2026-05-16

Snapshot of `CLAUDE.md` and `.claude/commands/*.md` taken right before change #2:

- Adding the `/spike` skill at `.claude/commands/spike.md` for throwaway experiments under `docs/project/spikes/NNN-<slug>/`.

Includes the change #1 state (CLAUDE.md has the verification iron law, `/orchestrate` has the hardened gates).

Restore with `/rollback 2026-05-16-pre-spike` to undo only change #2 (leaves change #1 in place).
