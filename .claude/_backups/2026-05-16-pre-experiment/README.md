## Pre-experiment snapshot — 2026-05-16

Snapshot of `.claude/commands/*.md` and `CLAUDE.md` taken before adopting:

1. CLAUDE.md "verification before completion" iron law + hardened orchestrator validation gates
2. `/spike` skill for throwaway-experiment workflow
3. Two-stage review (spec-reviewer + quality-reviewer) inside `/dev-story`

Restore with `/rollback 2026-05-16-pre-experiment` (or `/rollback latest` if this is still the most recent snapshot).

Companion git tag: `skills-pre-experiment`.
