# BMM Workflow Status - property-manager
# Generated by workflow-init
#
# STATUS DEFINITIONS:
# ==================
# Initial Status (before completion):
#   - required: Must be completed to progress
#   - optional: Can be completed but not required
#   - recommended: Strongly suggested but not required
#   - conditional: Required only if certain conditions met (e.g., if_has_ui)
#
# Completion Status:
#   - {file-path}: File created/found (e.g., "docs/product-brief.md")
#   - skipped: Optional/conditional workflow that was skipped

generated: "2025-11-28"
project: "property-manager"
project_type: "property-management-software"
selected_track: "bmad-method"
field_type: "greenfield"
workflow_path: "method-greenfield.yaml"

# User selections from workflow-init
discovery_selections:
  brainstorm: true
  research: true
  product_brief: true

# Project context captured during init
project_context:
  description: "Property management software for 14 rental units (5 current + 9 incoming). Core MVP focuses on expense tracking and tax reporting. Vision is maintainable and expandable platform."
  initial_scope: "expense tracking, tax reporting"
  growth_vision: "maintainable, expandable"

workflow_status:
  # Phase 0 - Discovery (Optional)
  - phase: 0
    phase_name: "Discovery"
    workflows:
      - id: "brainstorm-project"
        status: "docs/bmm-brainstorming-session-2025-11-28.md"
        agent: "analyst"
        command: "/bmad:bmm:workflows:brainstorm-project"

      - id: "research"
        status: "optional"
        agent: "analyst"
        command: "/bmad:bmm:workflows:research"
        note: "Can run multiple research workflows"

      - id: "product-brief"
        status: "optional"
        agent: "analyst"
        command: "/bmad:bmm:workflows:product-brief"
        note: "Recommended for greenfield Method projects"

  # Phase 1 - Planning
  - phase: 1
    phase_name: "Planning"
    workflows:
      - id: "prd"
        status: "docs/prd.md"
        agent: "pm"
        command: "/bmad:bmm:workflows:prd"
        output: "Product Requirements Document with FRs and NFRs"

      - id: "validate-prd"
        status: "optional"
        agent: "pm"
        command: "/bmad:bmm:workflows:validate-prd"
        note: "Quality check for PRD completeness"

      - id: "create-ux-design"
        status: "docs/ux-design-specification.md"
        agent: "ux-designer"
        command: "/bmad:bmm:workflows:create-ux-design"

  # Phase 2 - Solutioning
  - phase: 2
    phase_name: "Solutioning"
    workflows:
      - id: "architecture"
        status: "docs/architecture.md"
        agent: "architect"
        command: "/bmad:bmm:workflows:architecture"
        output: "System architecture document"

      - id: "create-epics-and-stories"
        status: "required"
        agent: "pm"
        command: "/bmad:bmm:workflows:create-epics-and-stories"
        note: "Break down PRD into implementable epics and stories"

      - id: "test-design"
        status: "recommended"
        agent: "tea"
        command: "/bmad:bmm:workflows:test-design"
        output: "System-level testability review"

      - id: "validate-architecture"
        status: "optional"
        agent: "architect"
        command: "/bmad:bmm:workflows:validate-architecture"
        note: "Quality check for architecture completeness"

      - id: "implementation-readiness"
        status: "required"
        agent: "architect"
        command: "/bmad:bmm:workflows:implementation-readiness"
        note: "Validates PRD + UX + Architecture + Epics cohesion"

  # Phase 3 - Implementation
  - phase: 3
    phase_name: "Implementation"
    workflows:
      - id: "sprint-planning"
        status: "required"
        agent: "sm"
        command: "/bmad:bmm:workflows:sprint-planning"
        note: "Creates sprint plan - subsequent work tracked in sprint-status.yaml"
